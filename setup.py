from setuptools import setup

setup(
   name='AWS',
   version='1.0',
   description='Aws SDk code',
   author='Khan',
   author_email='taukir707@gmail.com',
   packages=['AWS'],  #same as name
   install_requires=['wheel'], #external packages as dependencies
)